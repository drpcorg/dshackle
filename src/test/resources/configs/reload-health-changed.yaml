cluster:
  upstreams:
    - id: local1
      chain: ethereum
      labels:
        provider: provider1
      connection:
        ethereum-pos:
          execution:
            rpc:
              url: "http://localhost"
            ws:
              url: "ws://localhost"
    - id: local2
      chain: ethereum
      labels:
        provider: provider1
      connection:
        ethereum-pos:
          execution:
            rpc:
              url: "http://localhost"
            ws:
              url: "ws://localhost"
    - id: local3
      chain: polygon
      labels:
        provider: provider1
      connection:
        ethereum-pos:
          execution:
            rpc:
              url: "http://localhost"
            ws:
              url: "ws://localhost"
health:
  host: 0.0.0.0
  port: 10003
  path: /readyz
  blockchains:
    - chain: ethereum
      min-available: 2
    - chain: polygon
      min-available: 1
